import SwiftUI

struct ContentView: View {
    
    var body: some View {
        List {
            Text("Hello, World!")
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
