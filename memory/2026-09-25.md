# 2026-09-25

## Constitution + Clean Public API feature (feature 002)

- User asked: cải tiến public API cho clean theo React hooks/Atom/Riverpods + thêm rule + viết constitution cho dự án (State Management: Local + Global state, Swift).
- **Constitution v1.0.0 ratified** → `.specify/memory/constitution.md` (8 principles; V = Clean Public API non-negotiable). Commit `4abe343`.
- **Full speckit flow run for `specs/002-clean-public-api`** → commit `bbec1d2` (13/13 tasks):
  - Clarified: `watch()` → genuinely reactive (behavior fixed, not renamed); `addListener(for:)` deprecated; `read()` pure. Second clarify: 8 `preserved(by:)` overloads kept as-is + docs table (no deprecation).
  - `ProviderOverrideDiagnostics` helper for override type mismatches (fatalError with precise message; test the pure helper — can't XCTest a fatalError directly).
  - Audit shipped: `specs/002-clean-public-api/audit.md` (F1–F10; P3 deferred: deep review UI/Combine/Testing/Concurrency, SKSubscriberToken.Box surface, SC* prefix exception).
- **Lesson (container dispose semantics):** auto-dispose is transition-based — zero-listener elements are never swept; dispose only fires on 1→0 decrement (+cacheTime delay). Test with `listenersCount` + watch→removeListener cycle.
- **Lesson (Swift Testing):** `#require(x)` needs `try` and a `throws` test func; `@testable import` needed for internal helpers.
- Full suite after 002: **145 tests pass**, Examples build clean. Not pushed.

## Minor review findings still open (from 09-24, unfixed)

Function.swift duplicated in 2 modules; LRU O(n) accessOrder; dead defaultHasher; no removeObserver on ProviderContainer; GeolocationRollout always false; TTL cache double-callback onEvict+onExpire.
