# 2026-09-24

## Speckit workflow rule + installed workflows

- Added "Workflow: Speckit (Mandatory for Dev Tasks)" section to `AGENTS.md`: every dev task auto-runs the speckit skill flow (specify → clarify → plan → tasks → analyze → implement) without asking.
- At the human's request, installed 5 speckit workflows via `specify workflow add` (CLI at `~/.local/bin/specify`, spec-kit v0.12.14):
  - `pipeline` v1.1.0 (Guided SDD Pipeline — full cycle incl. clarify, analyze, converge loop) — from community catalog (discovery-only), installed from local YAML downloaded to /tmp
  - `yolo` v0.1.0 (no gates) — same community route
  - `speckit` v1.0.0 → v1.0.1 (updated from default catalog)
  - `bugfix` v1.0.0, `assess` v1.0.0 — from default catalog
- Patched `requires.integrations.any` in `speckit/pipeline/yolo` workflow.ymls to include `zcode` (project's integration) — keep this on future updates.
- Verified all 5 with `specify workflow info` — all parse and pass requires.
- Usage: `specify workflow run <id> -i spec="<desc>"` (bugfix/assess: `-i report=... -i slug=...`).
- Rule + workflow map documented in `AGENTS.md` → "Installed speckit workflows".
