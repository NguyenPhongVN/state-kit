import Testing
@testable import StateKitSupport

@Suite("StateKitSupport — Placeholder")
struct PlaceholderTests {
    @Test("Placeholder")
    func testPlaceholder() {
        #expect(Bool(true))
    }
}
